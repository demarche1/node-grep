let createReadStream,readline,clc;_1b7‍.x([["grep",()=>grep]]);_1b7‍.w("fs",[["createReadStream",["createReadStream"],function(v){createReadStream=v}]]);_1b7‍.w("readline",[["default",["readline"],function(v){readline=v}]]);_1b7‍.w("cli-color",[["default",["clc"],function(v){clc=v}]]);



       async function grep(args) {
  const cwd = process.cwd();
  const fileToRead = `${cwd}/${args.file}`;
  const streams = createReadStream(fileToRead);

  streams.on("error", () => {
    _1b7‍.g.console.error(clc.bgRed(clc.white(`${args.file} does not exists`)));
    process.exit(1);
  });

  const rl = readline.createInterface({
    input: streams,
    crlfDelay: Infinity,
  });

  for await (const line of rl) {
    _1b7‍.g.console.log(
      line.toString().replace(args.searchTerm, clc.red(args.searchTerm))
    );
  }
}
